import React from "react";
import ReactDOM from "react-dom/client";
import { PokeApp } from "../src/pages/PokeApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PokeApp />
  </React.StrictMode>
);
